feat(db): migracion portal_session y portal_audit_log

Crea tablas para auditar accesos del portal La Mundial:
- portal_session: registro de cada login (usuario, IP, token, expira)
- portal_audit_log: cada accion SSO (producto lanzado, parametros)
FK con cascada: usuario -> portal_session -> portal_audit_log
